# Cloud Agent Orchestrator — Environment Configuration
# Copy this file to .env and fill in the required values:
#   cp .env.example .env

# Flask secret key (optional; set for production session security)
# FLASK_SECRET_KEY=change-me-to-a-random-secret

# Port the application listens on (default: 5000)
# PORT=5000
